我们做两个阶段即可，第三第四不用那么快发展:
* 第一阶段：先解决目前的 Bug，让情绪数值真的能自然波动，不会长期锁满。
* 第二阶段：加入更严格的事件冷却，而不是一直依赖效果递减。

后面的阶段让你知道我们的目标就行，不用开始操作：
* 第三阶段：把固定 baseline 演进成可以随情境变化的 target。
* 第四阶段：把 mood 从「事件直接改数值」逐步改成「事件影响状态，状态影响数值」。
