1.整个开发过程中，请始终考虑 Token 成本，但不要为了节省 Token 而牺牲代码质量。

请你自行评估每一步的成本与收
益：

•⁠  ⁠如果一次较大的分析、测试或审查能够避免后续大量返工、Bug 修复或重复生成代码，即使这一步 Token 消耗较高，也请直接执行，因为整体成本更低。
•⁠  ⁠如果某些扫描、重复分析、重复读取文件或全仓库搜索没有必要，请主动采用更节省 Token 的方案，不要重复消耗。
•⁠  ⁠优先使用最小修改（Minimal Change）原则，只读取、分析和修改当前任务真正需要涉及的文件，而不是重新扫描整个项目。
•⁠  ⁠除非我明确要求，否则不要重新生成已经确认无误的代码，也不要重复解释已经讨论过的内容。
•⁠  ⁠如果存在两种实现方案，请默认选择综合成本最低（开发成本 + 后续维护成本 + Token 成本）的方案，而不是仅仅选择眼前 Token 最少的方法。
•⁠  ⁠如果你认为需要进行一次较大规模检查，请先判断它是否能够显著降低未来返工概率；如果答案是肯定的，请直接执行，不需要为了省 Token 而跳过。

对于连续开发，请始终以上一个完成的状态继续工作，不要重新阅读整个项目，除非你确认当前上下文已经不足以完成任务。

当发现问题时，先定位原因，再修改代码。不要通过大规模重构来解决局部问题。）

2.
•⁠  ⁠禁止修改 UI，除非我明确要求。
•⁠  ⁠一次只修一个功能。
•⁠  ⁠修改前必须列出影响文件。
•⁠  ⁠修改后必须自动运行检查。
•⁠  ⁠不允许因为猜测就重构。
•⁠  ⁠如果连续两次失败，先定位，不继续修改。

3.Project State:
用戶瀏覽器 (frontend.py)
    ↓ HTTP POST /watch
routes.py (FastAPI endpoint)
    ↓ 呼叫
brain.py (think_and_reply)
    ↓ 呼叫
deepseek_client.py (stream_chat)
    ↓ 呼叫
provider.py (實際 HTTP 請求到 DeepSeek API)
    ↓ SSE 串流回傳
    ↓
routes.py → 轉發 SSE events 給瀏覽器
    ↓
frontend.py processChunk() 解析並渲染

File Status:
| 檔案 | 狀態 | 說明 |
| `frontend.py` |  |  |
| `routes.py` | ✅ 確認正常 | SSE 轉發邏輯正常運作 |
| `brain.py` | ✅ 確認正常 | 不處理圖片,純文字對話邏輯正常 |
| `deepseek_client.py` | ✅ 確認正常 | 串流邏輯正常 |
| `provider.py` | ✅ 確認正常 | HTTP 請求層正常 |
| `state.py` | ✅ 確認正常 | 對話歷史存儲正常 |


已修復:

本轮已完成：聊天记录跨装置同步（手机dock/电脑dock/网页版共用同一份记录）

问题：三端各自读写浏览器 localStorage（key: lin_chat_v1），互不相通，
所以手机上聊的、电脑上看不到；刷新页面也只能看到当前这台设备自己存的记录。

改动（只影响这一件事，没碰 UI、没碰 SSE 相关逻辑）：
- config.py：新增 `CHAT_HISTORY_LIMIT`（默认500，环境变量可调，不用改数据库结构）
- db.py：新增 `load_conversations()` / `insert_conversation_turn()`，跟现有函数同一套
  try/except+`_client`判断的写法，没接 Supabase 时安静跳过、不报错
- state.py：`conversation_history` 从纯内存 deque(maxlen=50) 改成启动时从 Supabase
  读一份进内存（deque(maxlen=CHAT_HISTORY_LIMIT)），`add_conversation_turn()` 新增消息
  时同步写回 Supabase
- routes.py：新增 `GET /conversation`，返回字段对齐前端 renderMessages 需要的
  r/t/iso/time/think 格式
- frontend.py：`lchat()` 改名 `syncChat()`，页面载入时先打 `/conversation` 拿共享记录
  覆盖本地 localStorage 再渲染；请求失败（离线/后端没起来）静默 fallback 到原本的
  localStorage 内容，不影响离线可用性。`smsg()`/`addMsg()` 发消息逻辑本身没动。

需要跑的 SQL（去 Supabase SQL Editor 跑一次，之前的表不受影响）：

```sql
create table if not exists conversation_history (
  id bigint generated always as identity primary key,
  role text not null,
  content text not null,
  thinking text,
  image_data text,
  created_at timestamptz default now()
);
```

不影响现有功能：/watch /logs /memory 等既有接口对外行为不变，SSE 串流、
图片发送、思考框渲染都没有改动。

已验证：py_compile 语法检查、state.py/routes.py 独立载入、完整 app.main 载入、
TestClient 打 /health /conversation /logs（无 Supabase 时优雅返回空值不报错）、
写入一轮对话后 /conversation 能读出、frontend.py 两段 script 提取后过 Node 语法检查。

已知其他 bug：（无）
