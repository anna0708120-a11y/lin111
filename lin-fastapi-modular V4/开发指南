1.整个开发过程中，请始终考虑 Token 成本，但不要为了节省 Token 而牺牲代码质量。

请你自行评估每一步的成本与收
益：

•⁠  ⁠如果一次较大的分析、测试或审查能够避免后续大量返工、Bug 修复或重复生成代码，即使这一步 Token 消耗较高，也请直接执行，因为整体成本更低。
•⁠  ⁠如果某些扫描、重复分析、重复读取文件或全仓库搜索没有必要，请主动采用更节省 Token 的方案，不要重复消耗。
•⁠  ⁠优先使用最小修改（Minimal Change）原则，只读取、分析和修改当前任务真正需要涉及的文件，而不是重新扫描整个项目。
•⁠  ⁠除非我明确要求，否则不要重新生成已经确认无误的代码，也不要重复解释已经讨论过的内容。
•⁠  ⁠如果存在两种实现方案，请默认选择综合成本最低（开发成本 + 后续维护成本 + Token 成本）的方案，而不是仅仅选择眼前 Token 最少的方法。
•⁠  ⁠如果你认为需要进行一次较大规模检查，请先判断它是否能够显著降低未来返工概率；如果答案是肯定的，请直接执行，不需要为了省 Token 而跳过。

对于连续开发，请始终以上一个完成的状态继续工作，不要重新阅读整个项目，除非你确认当前上下文已经不足以完成任务。

当发现问题时，先定位原因，再修改代码。不要通过大规模重构来解决局部问题。）

2.
•⁠  ⁠禁止修改 UI，除非我明确要求。
•⁠  ⁠一次只修一个功能。
•⁠  ⁠修改前必须列出影响文件。
•⁠  ⁠修改后必须自动运行检查。
•⁠  ⁠不允许因为猜测就重构。
•⁠  ⁠如果连续两次失败，先定位，不继续修改。

3.Project State:
用戶瀏覽器 (frontend.py)
    ↓ HTTP POST /watch
routes.py (FastAPI endpoint)
    ↓ 呼叫
brain.py (think_and_reply)
    ↓ 呼叫
deepseek_client.py (stream_chat)
    ↓ 呼叫
provider.py (實際 HTTP 請求到 DeepSeek API)
    ↓ SSE 串流回傳
    ↓
routes.py → 轉發 SSE events 給瀏覽器
    ↓
frontend.py processChunk() 解析並渲染

File Status:
| 檔案 | 狀態 | 說明 |
| `frontend.py` | **正在修改** | 目前修 `send()` 的 SSE buffer 累積 bug |
| `routes.py` | ✅ 確認正常,**不能碰** | SSE 轉發邏輯正常運作 |
| `brain.py` | ✅ 確認正常,**不能碰** | 不處理圖片,純文字對話邏輯正常 |
| `deepseek_client.py` | ✅ 確認正常,**不能碰** | 串流邏輯正常 |
| `provider.py` | ✅ 確認正常,**不能碰** | HTTP 請求層正常 |
| `state.py` | ✅ 確認正常,**不能碰** | 對話歷史存儲正常 |


已修復;
send() 函式裡的 SSE buffer 累積 bug
第 744 行:let sseBuffer = ''; ✅
第 760-762 行:buffer 累積邏輯 ✅

修改 chatImageUpload handler (第 656 行):
改為只顯示預覽,不立即上傳
設置 pendingImageDataUrl 並顯示預覽列

新增 cancelImagePreview() 函式:
清空 pendingImageDataUrl
隱藏預覽列
清空 file input

新增 confirmImageSend() 函式:
完整 SSE 串流處理(reasoning + content + error)
與 send() 相同的思考框渲染邏輯
使用正確的 buffer 累積機制
清理:隱藏預覽、清空變數

消息被吞修復

3.思考框(reasoning)先出現
對話氣泡(content)接著獨立出現

已知其他 bug:
圖片失敗
圖片上傳回 500 錯誤本身的後端原因,還沒查
之前留在 main 上的那個 debug commit(7381221d)還沒清理
